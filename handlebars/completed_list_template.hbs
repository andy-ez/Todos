{{#each done_todos_by_date}}
  <dl data-title="{{@key}}" data-total="{{this.length}}" id="{{@key}}" class='completed'>
    <dt><time>{{@key}}</time></dt>
    <dd>{{this.length}}</dd>
  </dl>
{{/each}}